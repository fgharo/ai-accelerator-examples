apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: minio

resources:
  - namespace.yaml

helmCharts:
  - name: minio
    releaseName: minio-release
    namespace: minio
    version: 0.1.4
    repo: https://redhat-ai-services.github.io/fgharo/
    skipTests: true
    valuesFile: values.yaml
